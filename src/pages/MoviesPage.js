import {Genres, MoviesList} from "../components";
import {Fragment} from "react";

export const MoviesPage = () => {
    return (
        <Fragment>
            <Genres/>
            <MoviesList/>
        </Fragment>
    );
};
